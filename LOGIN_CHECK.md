# Проверка логина и пароля супервайзера

## Текущие значения (установлены в коде)

- **Логин:** `Amelie123`
- **Пароль:** `9512357*`

## Что было исправлено

1. ✅ Убраны подсказки (placeholders) из формы входа
2. ✅ Добавлено детальное логирование на сервере для отладки
3. ✅ Улучшена обработка пробелов и специальных символов
4. ✅ Добавлена проверка значений при старте сервера

## Где проверяются логин и пароль

Логин и пароль проверяются **только** в файле `server/index.js`:
- Строки 50-51: Определение значений
- Строки 220-250: Проверка при входе

**Важно:** Логин и пароль НЕ хранятся в MongoDB или JSON файлах. Они только в коде или в переменных окружения Vercel.

## Проверка на Vercel

Если логин не работает на Vercel, проверьте переменные окружения:

1. Зайдите в Vercel Dashboard
2. Ваш проект → Settings → Environment Variables
3. Проверьте наличие:
   - `SUPERVISOR_LOGIN` - должно быть `Amelie123` или удалить (чтобы использовалось значение по умолчанию)
   - `SUPERVISOR_PASSCODE` - должно быть `9512357*` или удалить

**Если переменные есть со старыми значениями - удалите их или обновите!**

## Отладка

При попытке входа сервер выводит в консоль:
- Ожидаемые значения
- Полученные значения
- Результат сравнения

Проверьте логи Vercel для диагностики проблем.

## Тестирование

Попробуйте войти с:
- Логин: `Amelie123` (без пробелов, точно так)
- Пароль: `9512357*` (без пробелов, со звездочкой в конце)







